import math

pi = 3.14

x = 1
y = 2
z = 3

print(round(pi))
print(math.ceil(pi)) # number rounded up to the nearest higher number
print(math.floor(pi)) # number rounded up to the nearest lower number
print(abs(pi))
print(pow(pi, 4))

print(max(x, y, z))